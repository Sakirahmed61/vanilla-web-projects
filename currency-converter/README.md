## Changelog

### Version 1.0:
- Simple currency converter that gets the input and displays the converted amount.
- Uses API to fetch the currency converted rates and calculates the amount with the specified rate.

uses free API from [ExchangeRate-API](https://www.exchangerate-api.com/)